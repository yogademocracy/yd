<iscontent type="text/html" charset="UTF-8" compact="true"/>

<isprint value="${pdict.regions.items
    .setClassName('wrapper ' + pdict.id + ' ' + (pdict.wrapperCSSClasses || ''))
    .setComponentAttribute("class", "wrapper__item")
    .render()}" encoding="off"/> 
